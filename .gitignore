# Database & runtime data (SQLite, WAL/SHM sidecars, snapshots, restore temp files).
# The dir is auto-created on startup (init_db) and by the Docker volume mount.
backend/data/
backend/finance.db
backend/*.db-wal
backend/*.db-shm

# Bank statement exports — contain personal financial data, never commit
csv files/
*.ml.pkl

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Frontend
web/node_modules/
web/.next/
web/test-results/
web/playwright-report/
web/*.tsbuildinfo
# Next artefacts égarés hors de web/ (un build lancé depuis le mauvais dossier)
.next/

# Environment
.env
.env.local

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Notes de travail (personnelles, non versionnées)
autre/
CLAUDE.md

